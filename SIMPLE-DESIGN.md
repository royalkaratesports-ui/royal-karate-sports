# Previous design superseded

The earlier simplified/overlay theme has been replaced by the coach-led Royal design documented in `DESIGN.md`.

Do not restore `site.css`, `simple.css`, `home.css` or earlier per-page styles into the generated pages. The canonical build loads only `royal.css` and `royal-pages.css`.

Current product direction: Akash Kishor Shinde leads the visual hierarchy as Founder & Head Coach; no repeated decorative section rules; original owner content, uncropped photos and honest local enquiry behavior are retained.
